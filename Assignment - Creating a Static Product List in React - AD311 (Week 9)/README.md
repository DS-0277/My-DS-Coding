**Assignment - Assignment - Creating a Static Product List in React - AD311 (Week 9)**


**1. Objective:** Build a static product list display using React, focusing on rendering a list from a provided dataset without involving state management. Note: While this project can be completed using Vite or Expo, the instructions below are specifically tailored for a Vite environment.

**2. Solution:** The solution to the problem is addressed in a directory named 'my-product-list' which is located inside another directory named 'create-product-list'. To run the program, we need to make sure we are located at 'my-product-list' by running the command prompt 'cd recipe-gallery'. We can check our status by running 'pwd' in the terminal. After that, we can run the server by running 'npm run dev'. The main file is named 'App.jsx' along with the test case file known as 'ProductList.test.jsx'. Additionally, the component used for building the app is addressed as 'ProductList.jsx'. One remarkable note is that the folder named 'node_modules' contained in the 'recipe-gallery' folder was not included due to large file size.

**3. YouTube Link:** The explanation is provided via the youtube link as follows: 
